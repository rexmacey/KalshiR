% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/historical.R
\name{get_historical_candlesticks}
\alias{get_historical_candlesticks}
\title{Get historical OHLC candlestick data for a market}
\usage{
get_historical_candlesticks(
  series_ticker,
  ticker,
  start_ts,
  end_ts,
  period_interval = 60L,
  creds = NULL
)
}
\arguments{
\item{series_ticker}{Series ticker (e.g. \code{"INXD"}). Required.}

\item{ticker}{Market ticker (e.g. \code{"INXD-23DEC29-T4000"}). Required.}

\item{start_ts}{Start of the candlestick window (POSIXct or ISO-8601).
Required.}

\item{end_ts}{End of the candlestick window. Required.}

\item{period_interval}{Period length in minutes. Common values: 1, 60, 1440.
Defaults to 60 (1 hour).}

\item{creds}{Credentials object or \code{NULL} for global.}
}
\value{
A tibble with one row per candle:
\describe{
\item{end_period_ts}{Candle close time (POSIXct UTC)}
\item{open_price}{Opening yes-price (0–1)}
\item{high_price}{Highest yes-price (0–1)}
\item{low_price}{Lowest yes-price (0–1)}
\item{close_price}{Closing yes-price (0–1)}
\item{volume}{Contracts traded in period}
\item{open_interest}{Open interest at period close}
}
}
\description{
Returns price candlestick data from the historical store. For recent data
use \code{\link[=get_market_candlesticks]{get_market_candlesticks()}} instead.
}
\details{
The Kalshi API requires a \code{series_ticker} AND \code{ticker} for this endpoint
(both are mandatory).
}
\examples{
\dontrun{
candles <- get_historical_candlesticks(
  series_ticker   = "INXD",
  ticker          = "INXD-23DEC29-T4000",
  start_ts        = as.POSIXct("2023-12-01", tz = "UTC"),
  end_ts          = as.POSIXct("2023-12-29", tz = "UTC"),
  period_interval = 1440L,
  creds           = creds
)
}
}
