% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/historical.R
\name{get_historical_fills}
\alias{get_historical_fills}
\title{Get historical fill (execution) records}
\usage{
get_historical_fills(
  ticker = NULL,
  order_id = NULL,
  min_ts = NULL,
  max_ts = NULL,
  limit = 100L,
  cursor = NULL,
  all_pages = FALSE,
  creds = NULL
)
}
\arguments{
\item{ticker}{Optional market ticker filter.}

\item{order_id}{Optional order ID filter.}

\item{min_ts}{Earliest fill time (POSIXct or ISO-8601).}

\item{max_ts}{Latest fill time.}

\item{limit}{Rows per page (default 100).}

\item{cursor}{Pagination cursor.}

\item{all_pages}{Fetch all pages automatically.}

\item{creds}{Credentials object or \code{NULL} for global.}
}
\value{
A tibble with the same schema as \code{\link[=get_fills]{get_fills()}}.
}
\description{
Like \code{\link[=get_fills]{get_fills()}} but queries the cold historical data store. Use
\code{\link[=get_historical_cutoff]{get_historical_cutoff()}} to determine which endpoint to use.
}
\examples{
\dontrun{
old_fills <- get_historical_fills(
  min_ts    = as.POSIXct("2023-01-01", tz = "UTC"),
  max_ts    = as.POSIXct("2023-06-01", tz = "UTC"),
  all_pages = TRUE,
  creds     = creds
)
}
}
