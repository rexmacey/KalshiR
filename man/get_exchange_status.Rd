% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exchange.R
\name{get_exchange_status}
\alias{get_exchange_status}
\title{Get the current exchange status}
\usage{
get_exchange_status(creds = NULL)
}
\arguments{
\item{creds}{A credentials object from \code{\link[=kalshi_auth]{kalshi_auth()}}. If \code{NULL} (default),
the globally stored credentials set by \code{\link[=kalshi_set_auth]{kalshi_set_auth()}} are used.}
}
\value{
A one-row tibble with columns:
\describe{
\item{trading_active}{Logical. \code{TRUE} if the exchange is open for trading.}
\item{exchange_active}{Logical. \code{TRUE} if the exchange is online at all.}
\item{condition}{Character. Human-readable status string (e.g. \code{"open"}).}
}
}
\description{
Returns a one-row tibble indicating whether the Kalshi exchange is currently
accepting orders, the trading status, and any maintenance information.
}
\examples{
\dontrun{
kalshi_set_auth(kalshi_auth(
  key_id      = Sys.getenv("KALSHI_KEY_ID"),
  private_key = Sys.getenv("KALSHI_KEY_PATH")
))

status <- get_exchange_status()
if (status$trading_active) {
  message("Exchange is open for trading.")
} else {
  message("Exchange is currently closed: ", status$condition)
}
}
}
