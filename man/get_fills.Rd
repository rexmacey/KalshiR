% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/portfolio.R
\name{get_fills}
\alias{get_fills}
\title{Get your trade fills (executions)}
\usage{
get_fills(
  ticker = NULL,
  order_id = NULL,
  min_ts = NULL,
  max_ts = NULL,
  limit = 100L,
  cursor = NULL,
  all_pages = FALSE,
  creds = NULL
)
}
\arguments{
\item{ticker}{Optional market ticker to filter fills.}

\item{order_id}{Optional order ID to filter fills.}

\item{min_ts}{Optional earliest fill timestamp (POSIXct or character
ISO-8601). Converted to milliseconds internally.}

\item{max_ts}{Optional latest fill timestamp.}

\item{limit}{Rows per page (default 100).}

\item{cursor}{Pagination cursor.}

\item{all_pages}{Fetch all pages automatically.}

\item{creds}{Credentials object or \code{NULL} for global.}
}
\value{
A tibble with columns:
\describe{
\item{fill_id}{Unique fill identifier}
\item{order_id}{The order this fill belongs to}
\item{ticker}{Market ticker}
\item{side}{\code{"yes"} or \code{"no"}}
\item{action}{\code{"buy"} or \code{"sell"}}
\item{count}{Contracts filled}
\item{yes_price}{Yes-side price in dollars (0–1)}
\item{no_price}{No-side price in dollars (0–1)}
\item{is_taker}{\code{TRUE} if your order was the liquidity taker}
\item{created_time}{Fill timestamp (POSIXct UTC)}
}
}
\description{
Returns a tibble of fill records — individual trade executions against
your orders. Fills represent the "done" portion of an order.
}
\examples{
\dontrun{
fills <- get_fills(ticker = "INXD-23DEC29-T4000", all_pages = TRUE,
                   creds = creds)
}
}
