% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/historical.R
\name{get_historical_trades}
\alias{get_historical_trades}
\title{Get historical public trade data for a market}
\usage{
get_historical_trades(
  ticker,
  min_ts = NULL,
  max_ts = NULL,
  limit = 100L,
  cursor = NULL,
  all_pages = FALSE,
  creds = NULL
)
}
\arguments{
\item{ticker}{Market ticker. Required.}

\item{min_ts}{Earliest trade time (POSIXct or ISO-8601).}

\item{max_ts}{Latest trade time.}

\item{limit}{Rows per page (default 100).}

\item{cursor}{Pagination cursor.}

\item{all_pages}{Fetch all pages automatically.}

\item{creds}{Credentials object or \code{NULL} for global.}
}
\value{
A tibble with columns:
\describe{
\item{trade_id}{Unique trade identifier}
\item{ticker}{Market ticker}
\item{side}{\code{"yes"} or \code{"no"}}
\item{count}{Contracts traded}
\item{yes_price}{Yes-side execution price (0–1)}
\item{no_price}{No-side execution price (0–1)}
\item{created_time}{Trade timestamp (POSIXct UTC)}
\item{taker_side}{\code{"yes"} or \code{"no"} — which side was the taker}
}
}
\description{
Returns the public trade tape from the historical store for a given
market. Each row is an individual anonymous trade (not your own — for
your fills see \code{\link[=get_historical_fills]{get_historical_fills()}}).
}
\examples{
\dontrun{
trades <- get_historical_trades(
  ticker    = "INXD-23DEC29-T4000",
  min_ts    = as.POSIXct("2023-12-01", tz = "UTC"),
  all_pages = TRUE,
  creds     = creds
)
}
}
