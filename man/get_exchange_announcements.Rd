% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exchange.R
\name{get_exchange_announcements}
\alias{get_exchange_announcements}
\title{Get exchange announcements}
\usage{
get_exchange_announcements(limit = 50L, creds = NULL)
}
\arguments{
\item{limit}{Integer. Maximum number of announcements to return. Default 50.}

\item{creds}{A credentials object from \code{\link[=kalshi_auth]{kalshi_auth()}}. If \code{NULL} (default),
the globally stored credentials set by \code{\link[=kalshi_set_auth]{kalshi_set_auth()}} are used.}
}
\value{
A tibble with columns:
\describe{
\item{id}{Character. Unique announcement ID.}
\item{title}{Character. Short title.}
\item{content}{Character. Full announcement text.}
\item{created_time}{POSIXct (UTC). When the announcement was posted.}
\item{type}{Character. Category (e.g. \code{"maintenance"}, \code{"general"}).}
}
Returns an empty tibble if there are no announcements.
}
\description{
Retrieves platform-level announcements from Kalshi (e.g. scheduled
maintenance, new feature notices, policy updates).
}
\examples{
\dontrun{
announcements <- get_exchange_announcements(limit = 10L)
print(announcements)
}
}
