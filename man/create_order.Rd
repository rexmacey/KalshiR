% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orders.R
\name{create_order}
\alias{create_order}
\title{Create a new order}
\usage{
create_order(
  ticker,
  side,
  action,
  count,
  type = "limit",
  yes_price = NULL,
  no_price = NULL,
  expiration_ts = NULL,
  client_order_id = NULL,
  creds = NULL
)
}
\arguments{
\item{ticker}{Character. Market ticker symbol (required).}

\item{side}{Character. \code{"yes"} or \code{"no"} (required).}

\item{action}{Character. \code{"buy"} or \code{"sell"} (required).}

\item{count}{Integer. Number of contracts (required).}

\item{type}{Character. Order type: \code{"limit"} or \code{"market"}. Default
\code{"limit"}.}

\item{yes_price}{Integer. Limit price in cents for a YES contract (0–99).
Required for limit orders. For a NO order, supply \code{no_price} instead.}

\item{no_price}{Integer. Limit price in cents for a NO contract (0–99).}

\item{expiration_ts}{POSIXct or integer. Order expiration time (epoch ms).
NULL means GTC (Good Till Cancelled).}

\item{client_order_id}{Character. Optional idempotency key (UUID). If
supplied and the same key is resubmitted, the API returns the original
order without creating a duplicate.}

\item{creds}{Credentials object or NULL.}
}
\value{
A one-row tibble describing the created order. Prices in dollars,
timestamps as POSIXct UTC.
}
\description{
Submits a new buy or sell order for a market. Returns the created order.
}
\details{
For limit orders you must supply either \code{yes_price} OR \code{no_price} (they
must sum to 100). The API will reject orders where both are supplied and
inconsistent.

Kalshi uses cents (1–99) for limit prices; do \strong{not} pass dollar values
here.
}
\examples{
\dontrun{
# Buy 10 YES contracts at 45 cents each
create_order(
  ticker    = "INXD-24DEC31-T5000",
  side      = "yes",
  action    = "buy",
  count     = 10L,
  yes_price = 45L
)

# Market buy — no price required
create_order(
  ticker = "INXD-24DEC31-T5000",
  side   = "yes",
  action = "buy",
  count  = 5L,
  type   = "market"
)
}

}
