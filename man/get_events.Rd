% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/markets.R
\name{get_events}
\alias{get_events}
\title{Get a list of events}
\usage{
get_events(
  series_ticker = NULL,
  status = NULL,
  category = NULL,
  limit = 100L,
  all_pages = FALSE,
  creds = NULL
)
}
\arguments{
\item{series_ticker}{\code{character(1)} or \code{NULL}. Filter to a specific series.}

\item{status}{\code{character(1)} or \code{NULL}. One of \code{"open"}, \code{"closed"},
\code{"settled"}, or \code{NULL} for all.}

\item{category}{\code{character(1)} or \code{NULL}. Filter by category.}

\item{limit}{\code{integer(1)}. Results per page. Default 100.}

\item{all_pages}{\code{logical(1)}. Fetch all pages? Default \code{FALSE}.}

\item{creds}{A \code{kalshi_credentials} object or \code{NULL}.}
}
\value{
A \code{tibble} with one row per event.
}
\description{
Returns events — groupings of related markets — optionally filtered by
series, status, or category.
}
\examples{
\dontrun{
get_events(series_ticker = "KXHIGHNY", status = "open")
get_events(category = "Politics", all_pages = TRUE)
}

}
