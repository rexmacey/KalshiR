% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orders.R
\name{get_orders}
\alias{get_orders}
\title{Get orders}
\usage{
get_orders(
  ticker = NULL,
  event_ticker = NULL,
  status = NULL,
  min_ts = NULL,
  max_ts = NULL,
  limit = 100L,
  cursor = NULL,
  all_pages = FALSE,
  creds = NULL
)
}
\arguments{
\item{ticker}{Character. Filter to a specific market ticker.}

\item{event_ticker}{Character. Filter to markets within a specific event.}

\item{status}{Character. Filter by order status: \code{"resting"},
\code{"canceled"}, \code{"executed"}, \code{"pending"}. NULL returns all.}

\item{min_ts}{POSIXct or integer. Earliest order creation timestamp.}

\item{max_ts}{POSIXct or integer. Latest order creation timestamp.}

\item{limit}{Integer. Results per page (max 1000). Default 100.}

\item{cursor}{Character. Pagination cursor.}

\item{all_pages}{Logical. Fetch all pages? Default FALSE.}

\item{creds}{Credentials object or NULL.}
}
\value{
A tibble of orders with columns:
\describe{
\item{order_id}{Unique order identifier.}
\item{ticker}{Market ticker symbol.}
\item{event_ticker}{Parent event ticker.}
\item{side}{\code{"yes"} or \code{"no"}.}
\item{action}{\code{"buy"} or \code{"sell"}.}
\item{type}{\code{"limit"} or \code{"market"}.}
\item{status}{Current order status.}
\item{count}{Original number of contracts.}
\item{remaining_count}{Unfilled contracts.}
\item{yes_price}{Limit price in dollars.}
\item{no_price}{Implied NO price in dollars.}
\item{created_time}{POSIXct UTC — order creation time.}
\item{expiration_time}{POSIXct UTC — expiration time (if set).}
}
}
\description{
Returns orders for the authenticated user. Supports filtering by market,
status, and time range.
}
\examples{
\dontrun{
# All resting orders
get_orders(status = "resting")

# All orders for a market
get_orders(ticker = "INXD-24DEC31-T5000", all_pages = TRUE)
}

}
