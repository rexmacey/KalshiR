% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/historical.R
\name{get_historical_orders}
\alias{get_historical_orders}
\title{Get historical order records}
\usage{
get_historical_orders(
  ticker = NULL,
  event_ticker = NULL,
  min_ts = NULL,
  max_ts = NULL,
  status = NULL,
  limit = 100L,
  cursor = NULL,
  all_pages = FALSE,
  creds = NULL
)
}
\arguments{
\item{ticker}{Optional market ticker filter.}

\item{event_ticker}{Optional event ticker filter.}

\item{min_ts}{Earliest order creation time.}

\item{max_ts}{Latest order creation time.}

\item{status}{Order status filter: \code{"resting"}, \code{"canceled"},
\code{"executed"}, \code{"pending"}.}

\item{limit}{Rows per page (default 100).}

\item{cursor}{Pagination cursor.}

\item{all_pages}{Fetch all pages automatically.}

\item{creds}{Credentials object or \code{NULL} for global.}
}
\value{
A tibble with the same schema as \code{\link[=get_orders]{get_orders()}}.
}
\description{
Like \code{\link[=get_orders]{get_orders()}} but queries the historical data store.
}
\examples{
\dontrun{
old_orders <- get_historical_orders(
  status    = "executed",
  min_ts    = as.POSIXct("2023-01-01", tz = "UTC"),
  all_pages = TRUE,
  creds     = creds
)
}
}
