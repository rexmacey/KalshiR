% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/portfolio.R
\name{get_positions}
\alias{get_positions}
\title{Get your current open positions}
\usage{
get_positions(limit = 100L, cursor = NULL, all_pages = FALSE, creds = NULL)
}
\arguments{
\item{limit}{Maximum rows per page (default 100, max 1000).}

\item{cursor}{Pagination cursor from a previous call. Leave \code{NULL} to
start from the beginning.}

\item{all_pages}{If \code{TRUE}, automatically fetches all pages and returns
a single combined tibble. Ignores \code{cursor} when \code{TRUE}.}

\item{creds}{A credentials object from \code{\link[=kalshi_auth]{kalshi_auth()}}. If \code{NULL}, uses
the globally stored credentials.}
}
\value{
A tibble with one row per open position. Columns include:
\describe{
\item{ticker}{Market ticker symbol}
\item{event_ticker}{Parent event ticker}
\item{side}{\code{"yes"} or \code{"no"}}
\item{position}{Net contract count (positive = long)}
\item{market_exposure_cents}{Dollar value at risk}
\item{realized_pnl}{Realised profit/loss in dollars}
\item{unrealized_pnl}{Unrealised profit/loss in dollars}
\item{total_traded}{Total contracts traded in this market}
\item{fees_paid}{Total fees paid in dollars}
}
}
\description{
Returns a tibble of all open market positions for the authenticated user.
Use \code{all_pages = TRUE} to retrieve every position automatically.
}
\examples{
\dontrun{
positions <- get_positions(all_pages = TRUE, creds = creds)
}
}
