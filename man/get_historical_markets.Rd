% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/historical.R
\name{get_historical_markets}
\alias{get_historical_markets}
\title{Get historical market snapshots}
\usage{
get_historical_markets(
  event_ticker = NULL,
  series_ticker = NULL,
  status = NULL,
  min_close_ts = NULL,
  max_close_ts = NULL,
  limit = 100L,
  cursor = NULL,
  all_pages = FALSE,
  creds = NULL
)
}
\arguments{
\item{event_ticker}{Filter by parent event ticker.}

\item{series_ticker}{Filter by series ticker.}

\item{status}{Filter by market status at the historical time.
One of \code{"open"}, \code{"closed"}, \code{"settled"}.}

\item{min_close_ts}{Only return markets that closed after this time
(POSIXct or ISO-8601 string).}

\item{max_close_ts}{Only return markets that closed before this time.}

\item{limit}{Rows per page (default 100).}

\item{cursor}{Pagination cursor.}

\item{all_pages}{Fetch all pages automatically.}

\item{creds}{Credentials object or \code{NULL} for global.}
}
\value{
A tibble of historical market records. Columns mirror
\code{\link[=get_markets]{get_markets()}} output but reflect the historical snapshot.
}
\description{
Returns market metadata snapshots from the historical data store. Useful
for reconstructing what markets existed and their parameters at a given
time in the past.
}
\examples{
\dontrun{
hist_markets <- get_historical_markets(
  series_ticker = "INXD",
  status        = "settled",
  all_pages     = TRUE,
  creds         = creds
)
}
}
