% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orders.R
\name{batch_create_orders}
\alias{batch_create_orders}
\title{Batch create orders}
\usage{
batch_create_orders(orders, creds = NULL)
}
\arguments{
\item{orders}{A list of order specification lists. Each element should
be a named list with the same fields accepted by \code{\link[=create_order]{create_order()}}:
\code{ticker}, \code{side}, \code{action}, \code{count}, \code{type}, \code{yes_price}/\code{no_price},
and optionally \code{expiration_ts} and \code{client_order_id}.}

\item{creds}{Credentials object or NULL.}
}
\value{
A tibble with one row per submitted order.
}
\description{
Submits up to 20 orders in a single API call. More efficient than
calling \code{\link[=create_order]{create_order()}} in a loop when placing many orders.
}
\details{
The Kalshi API processes batch orders atomically per order — a failure
on one order does not roll back the others. Check the \code{status} column of
the returned tibble to identify any orders that were rejected.
}
\examples{
\dontrun{
orders <- list(
  list(
    ticker    = "INXD-24DEC31-T5000",
    side      = "yes",
    action    = "buy",
    count     = 5L,
    yes_price = 45L
  ),
  list(
    ticker    = "INXD-24DEC31-T4800",
    side      = "no",
    action    = "buy",
    count     = 3L,
    no_price  = 60L
  )
)
batch_create_orders(orders)
}

}
